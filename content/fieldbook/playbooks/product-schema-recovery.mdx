---
title: Product schema recovery after a theme change
navTitle: Product schema recovery
summary: Repair product markup after a theme, app, or storefront deployment changes the rendered graph.
category: Storefront operations
kind: playbook
status: stable
order: 2
reviewedAt: 2026-07-25
relatedSkills:
  - product-structured-data-validation
relatedResources:
  - schema-org
  - json-ld-11
  - google-product-structured-data
audienceNote: "A developer or theme-agency sequence — your job is to confirm it's assigned after a release and sign off on Done when."
---

This sequence applies when structured-data diagnostics regress after a storefront release.

## Capture before editing

Save the rendered HTML, parser output, visible product facts, active variant, market, and a previous known-good page when one is available.

## Steps

1. List every `Product`, `ProductGroup`, and `Offer` entity.
2. Find the theme, app, or extension that emits each one.
3. Compare the graph with the product facts shown to the shopper.
4. Remove or consolidate duplicate emitters.
5. Repair variant and offer relationships.
6. Test the rendered HTML in preview.
7. Deploy through the normal review path.
8. Check the live page and the external diagnostic again.

<Callout type="boundary" title="Search display is still external">
A coherent implementation is the part you control. Search engines decide whether and how to display it.
</Callout>
